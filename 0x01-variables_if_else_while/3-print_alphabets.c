#include <stdio.h>
/**
 * main - prints alpha in lower and upper case
 * Return: Always 0 (Success)
 */
int main(void)
{
	char upr;
	char lwr;
	
	for (lwr = 'a'; ch <= 'z'; lwr++)
		putchar(lwr);
	for (upr = 'A'; ch <= 'Z'; upr++)
		putchar(upr);

	putchar('\n');
	return (0);
}
